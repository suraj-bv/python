n = input('Enter the no. of items to insert: ')

items = []

for i in range(n):
    item = input(f'Enter item {i+1}: ')
    items.append(item)

def reverse(items):
    while(len(items) >-1):
        items[]